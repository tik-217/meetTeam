import { LoginWidget } from "@/widgets/LoginWidget";
import "./Authentication.scss";

export const Authentication = () => {
  return (
    <main className="auth">
      <LoginWidget />
    </main>
  );
};
